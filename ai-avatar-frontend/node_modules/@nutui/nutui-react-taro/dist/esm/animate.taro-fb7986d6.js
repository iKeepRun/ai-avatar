import React__default, { useState } from 'react';
import classNames from 'classnames';
import { c as cn } from './bem-350c1702.js';

const defaultProps = {
    type: 'shake',
    action: 'initial',
    loop: false,
    onClick: (event) => { },
};
const Animate = (props) => {
    const { className, type, action, loop, onClick, children, ...rest } = {
        ...defaultProps,
        ...props,
    };
    const [clicked, setClicked] = useState(false);
    const b = cn('animate');
    const classes = classNames({
        'nut-ani-container': true,
        [`${b('')}-${type}`]: action === 'initial' || clicked ? type : false,
        loop: loop,
    });
    const cls = classNames(classes, className);
    const handleClick = (event) => {
        setClicked(true);
        //如果不是无限循环，清除类名
        if (!loop) {
            setTimeout(() => {
                setClicked(false);
            }, 1000);
        }
        onClick && onClick(event);
    };
    return (React__default.createElement("div", { className: "nut-animate" },
        React__default.createElement("div", { className: cls, onClick: handleClick, ...rest }, children)));
};
Animate.defaultProps = defaultProps;
Animate.displayName = 'NutAnimate';

export { Animate as A };
