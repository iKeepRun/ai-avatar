import React__default from 'react';
import { c as cn } from './bem-350c1702.js';
import { u as useConfig } from './configprovider.taro-8d71eb49.js';

const defaultProps = {
    className: '',
    date: '',
    curKey: 0,
};
const TimePannel = (props) => {
    useConfig();
    const { children, className, date, curKey, change } = {
        ...defaultProps,
        ...props,
    };
    const b = cn('timepannel');
    return (React__default.createElement("div", { className: `${b()} ${className || ''}`, onClick: () => change(curKey) }, date));
};
TimePannel.defaultProps = defaultProps;
TimePannel.displayName = 'NutTimePannel';

export { TimePannel as T };
